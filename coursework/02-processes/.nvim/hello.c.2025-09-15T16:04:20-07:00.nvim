#include <stdbool.h>
#include <stdio.h>
#include <unistd.h>

int main() {
  fork() printf("Start :PID=",  getpid() "parent PID=",  getppid());

  /**
  printf("outside the loop");
  fork();
  for (int i = 0; i < 10; i++) {
    sleep(1);
    printf("i \n");
  }
  fork();
  **/
}
